using System.Collections;
using System.Collections.Generic;
using Godot;

public partial class LinkModuleLink : Node
{
    // [SerializeField] private Renderer _borderRenderer;
    // [SerializeField] private RuneVisualizer _rune;

    public void Set(LinkModuleData data)
    {
        // _borderRenderer.material = data.RandomMaterial();
        // _rune.Set(data.RandomRune());
    }
}
